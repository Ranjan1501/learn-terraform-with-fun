# Learn Terraform with fun

.auto.tfvars files are detected by terraform automatically so be careful using assinging value or configuration in these files.

# .auto.tfvars It has more precedence over terraform.tfvars

# Variable use in auto.tfvars will override that use in .tfvars

# -var -var-file cli arguments have more precedence over .auto.tfvars

